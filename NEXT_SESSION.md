# Next session — pick up here

**Branch:** `t07-t15-implementation`, branched off `origin/main` at `0079f71`
(merge of decision #11's fix). This branch is for T07–T15 code only — no
plan-document changes bundled in. `main` stays frozen at T06 + decision #11
until this branch is merged back.

**Baseline verified before any T07-T15 work started:** full suite —
227/227 passing.

**Current task:** T07 — `tasks/t07_materializer_ids_idempotency.md`.
Not yet opened or started this session. Read that file first; it has its
own Done-when checklist to work through.

**Unresolved blockers:** none. `blockers.md` §1 (Blockers) and §3 (Bugs) are
both empty.

**Decisions taken but not yet written to disk:** none. Everything decided
through decision #11 is already committed and merged to `main`.

**How to resume:**
1. Confirm you're on `t07-t15-implementation` (`git status`).
2. Run the full suite to reconfirm the 227-passing baseline before touching
   anything.
3. Open `tasks/t07_materializer_ids_idempotency.md` and start there.
4. Delete this file (or update it) once T07 is committed — it's a handoff
   note for the next session, not a permanent record like `blockers.md`.
