Completed task files land here.

When you finish a task in `tasks/`, tick every "Done when" box, commit,
then move the file into this folder. The file becomes the record of what
was actually built — if the task turned out to be wrong, edit it before
moving it rather than moving a description that no longer matches reality.
